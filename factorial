#include <stdio.h>

int main() {
    int num, i = 1;
    long long factorial = 1;

    printf("Enter a number: ");
    scanf("%d", &num);

    while (i <= num) {
        factorial = factorial * i;
        i++;
    }

    printf("Factorial = %lld", factorial);

    return 0;
}
